@model IEnumerable<QMAH.Web.Areas.User.ViewModels.PointTransactionListItemViewModel>

@{
    ViewData["Title"] = "點數流水";
    int currentPage = ViewBag.CurrentPage;
    int totalPages = ViewBag.TotalPages;
    int totalCount = ViewBag.TotalCount;
    int pageSize = ViewBag.PageSize;

    int start = totalCount == 0 ? 0 : ((currentPage - 1) * pageSize) + 1;
    int end = Math.Min(currentPage * pageSize, totalCount);
}

@section PageActions {
    <div class="qmah-actions d-flex flex-wrap gap-2">
        <a asp-area="User" asp-controller="PointBackpack" asp-action="Index" class="btn btn-outline-secondary">
            <i class="ti ti-wallet me-1"></i>點數背包
        </a>
        <a asp-area="User" asp-controller="PointTransactions" asp-action="Index" class="btn btn-secondary">
            <i class="ti ti-receipt me-1"></i>點數流水
        </a>
    </div>
}

<div class="card qmah-filter-panel mb-3">
    <div class="card-header">
        <div>
            <h2 class="card-title">查詢點數流水</h2>
            <div class="card-subtitle">依會員、異動原因或點數方向篩選紀錄。</div>
        </div>
    </div>
    <div class="card-body">
        <form method="get" asp-action="Index" class="qmah-filter-form">
            <div class="row g-2">
                <div class="col-md-6">
                    <input type="text"
                           name="keyword"
                           value="@ViewBag.Keyword"
                           class="form-control"
                           placeholder="搜尋 Email、原因或異動類型" />
                </div>

                <div class="col-md-3">
                    <select name="amountType" class="form-select">
                        <option value="">全部異動</option>
                        <option value="increase" selected="@(ViewBag.AmountType == "increase")">增加點數</option>
                        <option value="decrease" selected="@(ViewBag.AmountType == "decrease")">扣除點數</option>
                    </select>
                </div>

                <div class="col-md-3 d-flex align-items-end gap-2">
                    <button type="submit" class="btn btn-primary flex-fill">
                        <i class="ti ti-search me-1"></i>搜尋
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">清除</a>
                </div>
            </div>
        </form>
    </div>
</div>

<div class="card qmah-list-card">
    <div class="card-header qmah-crud-toolbar">
        <div>
            <h2 class="card-title">流水紀錄</h2>
            <div class="card-subtitle">共 @totalCount 筆</div>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-vcenter card-table qmah-crud-table">
            <thead>
                <tr>
                    <th>時間</th>
                    <th>會員</th>
                    <th>異動</th>
                    <th>原因</th>
                    <th>類型</th>
                </tr>
            </thead>

            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5">
                            <div class="empty qmah-empty-state">
                                <div class="empty-icon"><i class="ti ti-receipt-off"></i></div>
                                <p class="empty-title">目前沒有符合條件的紀錄</p>
                                <p class="empty-subtitle text-secondary">請調整搜尋條件後再試一次。</p>
                            </div>
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-nowrap">
                                <span class="fw-semibold">@item.Transaction.CreatedAt.ToLocalTime().ToString("yyyy/MM/dd")</span>
                                <div class="text-secondary small">@item.Transaction.CreatedAt.ToLocalTime().ToString("HH:mm")</div>
                            </td>

                            <td>@item.Email</td>

                            <td>
                                @if (item.Transaction.Amount > 0)
                                {
                                    <span class="badge bg-green-lt">+@item.Transaction.Amount</span>
                                }
                                else
                                {
                                    <span class="badge bg-red-lt">@item.Transaction.Amount</span>
                                }
                            </td>

                            <td>@item.Transaction.Reason</td>
                            <td><span class="badge bg-secondary-lt">@item.Transaction.ReferenceType</span></td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

<div class="qmah-pagination-bar d-flex flex-wrap justify-content-between align-items-center gap-3 mt-3">
    <div class="text-muted">顯示第 @start～@end 筆，共 @totalCount 筆</div>

    <nav aria-label="點數流水分頁">
        <div class="btn-list">
            @if (currentPage > 1)
            {
                <a asp-action="Index"
                   asp-route-page="@(currentPage - 1)"
                   asp-route-keyword="@ViewBag.Keyword"
                   asp-route-amountType="@ViewBag.AmountType"
                   class="btn btn-secondary">上一頁</a>
            }

            @for (int i = 1; i <= totalPages; i++)
            {
                <a asp-action="Index"
                   asp-route-page="@i"
                   asp-route-keyword="@ViewBag.Keyword"
                   asp-route-amountType="@ViewBag.AmountType"
                   class="btn btn-sm @(i == currentPage ? "btn-primary" : "btn-outline-secondary")">@i</a>
            }

            @if (currentPage < totalPages)
            {
                <a asp-action="Index"
                   asp-route-page="@(currentPage + 1)"
                   asp-route-keyword="@ViewBag.Keyword"
                   asp-route-amountType="@ViewBag.AmountType"
                   class="btn btn-secondary">下一頁</a>
            }
        </div>
    </nav>
</div>
