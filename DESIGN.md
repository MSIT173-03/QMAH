# QMAH 後台介面規範

本文件是 QMAH 管理後台的共同介面基準。後台的主要工作是查閱資料、判斷狀態、執行管理操作與追蹤結果，因此畫面優先提供清楚的資訊階層、可預期的操作位置與足夠的錯誤回饋；不把前台社群的裝飾性互動直接搬進管理畫面。

## 產品定位

- 後台使用者是需要快速完成管理工作的管理員、內容編輯與營運人員。
- 頁面採用同一個 Admin shell，包含側邊導覽、頂部工具列、主要內容區與頁尾。
- 五大系統維持既有分區：圖鑑、遊戲、社群、商城與會員；營運中心、匯入工具與媒體管理屬於共用管理功能。
- 這份規範只約束管理後台，不開始實作前台頁面，也不取代各 Area 的資料與權限規則。

## 頁面結構

一般管理頁面依下列順序組成：

1. 頁面標題與一句話說明目前任務。
2. 需要快速判讀時先放摘要卡；摘要卡要能通往完整明細，不只呈現無法操作的數字。
3. 篩選器使用 `qmah-filter-panel`，欄位名稱要能直接說明搜尋範圍，送出與清除操作固定放在操作列。
4. 資料列表使用 `qmah-list-card` 或 `qmah-crud-table`，長文字截斷時仍保留完整詳細頁入口。
5. 建立、編輯與刪除操作集中在 `qmah-card-footer`；分頁與筆數摘要使用 `qmah-card-footer--split`。

詳細頁先顯示核心識別與狀態，再按資訊責任分成幾個 `qmah-detail-section`。不把所有欄位塞在同一張表，也不在不同 Area 重新發明另一套卡片、表單或按鈕排版。

## 視覺與排版

- 使用既有 QMAH tokens 與區域主色，新增顏色前先確認是否能由現有 token 表達。
- 標題、摘要、欄位標籤與輔助說明保持固定層級；一個區塊只保留一個主要標題。
- 卡片以邊界、留白與標題階層分組，不用大量陰影或 hover 位移製造層次。
- 主要操作使用實心按鈕，次要操作使用低強度樣式，刪除與不可逆操作固定使用危險色並在送出前確認。
- 表格欄位先顯示管理員需要判斷的名稱、狀態、時間與操作；長識別碼只在確實需要複製或追查時顯示。
- 資料庫代碼要透過顯示文字對照表轉成自然的繁體中文，不能把 `POST_TYPE`、`REVIEW_STATUS` 這類內部鍵值直接當成主要 UI 文案。

## 操作狀態

所有互動元件至少要能辨識一般、hover、focus-visible、按下、停用、載入、成功與失敗狀態。動畫只用來表示狀態變化與操作結果，時間保持短且不阻礙工作；使用者開啟減少動態效果時，轉場與動畫必須降到最低。

表單送出後要避免重複提交；失敗時在欄位附近保留可理解的錯誤原因，成功時提供下一步或返回列表的明確路徑。空資料、查無結果、權限不足、資料已被其他人修改與網路／伺服器錯誤都要有可讀的狀態說明。

## RWD 與可及性

- 內容區以流動寬度為主，表格在窄螢幕可橫向閱讀，但頁面標題、篩選器與主要操作不應被迫藏在水平捲軸後。
- 表單欄位在小螢幕改為單欄排列；操作按鈕可滿寬排列，仍維持一致的順序與危險操作辨識。
- 導覽在小螢幕收合時仍保留目前位置、返回方式與鍵盤可操作性。
- 互動元件要有可見的鍵盤焦點，圖像要有合適替代文字，圖表要有文字摘要或明細表格作為輔助閱讀方式。
- 色彩不能成為辨識狀態的唯一方式；狀態同時使用文字、圖示或結構提示。

## 營運與資料展示

營運中心採「先概觀、再明細」：摘要卡顯示目前範圍與重要指標，點擊後進入同一指標的趨勢圖、摘要數值、逐日資料與匯出功能。圖表不是數字卡片的裝飾，必須能回答時間變化、資料範圍與目前數值代表什麼。

展示資料需要維持跨系統關聯。社群貼文只有部分連到文物，其他內容可來自遊戲、活動、平台使用經驗與官方公告；訂單只使用與文物關聯的縮小複製品，並保留成交時的商品名稱與單價快照。資料工具的展示資料不可用重複標題或無意義的 `XXXX` 取代實際可閱讀內容。

商城商品除了規格與價格，也保留會員的星等和簡短心得；公開摘要只納入已發布評價，會員可以維護自己的內容。遊戲則保留房間、回合、回答、投票與結算關聯，結果頁可同時呈現每回合排名、勝者、票數和整場排行榜，讓前台未來不必自行拼湊歷程或猜測分數。

## CSS 維護方式

共用 CSS 依責任分層：`qmah-admin-core.css` 管理版面基礎與共用元件，`qmah-admin-navigation.css` 管理導覽，`qmah-admin-footer.css` 管理頁尾，`qmah-operations.css` 管理營運中心特有元件，各 Area 樣式只處理該區必要的差異。每個區塊上方以繁體中文註解說明「這段規則影響什麼」，不要只留下無法判斷用途的選擇器名稱。

新增頁面時先套用既有類別，再考慮補充小範圍樣式。避免在 Razor 內加入固定寬度、顏色或間距的 inline style；動態數值若確實需要 inline custom property，必須限制在單一資料呈現用途並提供合理的預設值。

## 完成前自我檢查

- 頁面是否能在不熟悉資料庫欄位名稱的情況下理解？
- 主要操作是否只有一個清楚的視覺優先順序？
- 篩選、列表、詳細頁、表單與頁尾是否沿用共用結構？
- 空資料、錯誤、長文字、窄螢幕、鍵盤焦點與減少動態效果是否有對應狀態？
- 圖表是否能回到完整資料，而不是只顯示一張無法查證的圖片？
- 是否保留跨系統關聯，並避免把展示資料誤寫成真實販售或研究結論？
