# 公告/活動跑馬燈 — 功能提案（待會議確認）

這份文件只是規劃，**還沒有動資料庫或寫程式**。需要的欄位、新增的 API、前端頁面先列出來，開會確認方向後再動工。

## 現況釐清

- 貼文牆看到的「公告」其實是 `social.SocialPosts` 裡 `PostType = 'ANNOUNCEMENT'` 的資料，不是資料庫裡 `social.OfficialAnnouncements` 那張表——那張表目前完全沒有被 API 用到。
- 活動資料在 `social.Events`。
- 兩張表現在都**沒有**「要不要放上跑馬燈」「排第幾個」這類欄位。

## 兩個做法，取捨不同

### 方案 A：簡單 bit 欄位（今天能做完，但功能陽春）

```sql
ALTER TABLE social.SocialPosts ADD IsFeatured bit NOT NULL CONSTRAINT DF_SocialPosts_IsFeatured DEFAULT 0;
ALTER TABLE social.Events      ADD IsFeatured bit NOT NULL CONSTRAINT DF_Events_IsFeatured      DEFAULT 0;
```

- 優點：改動小，管理頁只要一個開關 checkbox。
- 缺點：
  - 跑馬燈裡的排序沒得控制（只能照 CreatedAt/StartAt 之類的欄位排）。
  - 沒有「這則公告只在跑馬燈上顯示到某天」的排程功能。
  - 同一則內容不能重複出現、也不能只在特定時段展示。

### 方案 B：獨立的 CarouselItems 表（彈性較高，工程量較大）

```sql
CREATE TABLE social.CarouselItems (
    Id               uniqueidentifier NOT NULL CONSTRAINT PK_CarouselItems PRIMARY KEY,
    ContentType      nvarchar(20)     NOT NULL, -- 'POST'（含公告）或 'EVENT'
    ContentId        uniqueidentifier NOT NULL, -- 對應 SocialPosts.Id 或 Events.Id（跨表，無法用單一 FK 限制，靠程式邏輯檢查）
    DisplayOrder     int              NOT NULL,
    StartAt          datetime2(3)     NULL,      -- 開始展示時間，NULL 代表現在就開始
    EndAt            datetime2(3)     NULL,       -- 結束展示時間，NULL 代表不自動下架
    CreatedByUserId  uniqueidentifier NOT NULL,
    CreatedAt        datetime2(3)     NOT NULL CONSTRAINT DF_CarouselItems_Created DEFAULT (sysutcdatetime()),
    CONSTRAINT CK_CarouselItems_ContentType CHECK (ContentType IN ('POST','EVENT'))
);
```

- 優点：可以排序（拖拉調整 `DisplayOrder`）、可以排程上下架、不用動既有兩張表的 schema。
- 缺點：要多寫一組管理 CRUD API + 拖拉排序的前端 UI，工程量比方案 A 大。

**建議**：如果只是要「先有跑馬燈能動」，選方案 A；如果预期公告/活動會常態性地排跑馬燈、需要控制順序和上下架時間，直接做方案 B，不然之後還要再遷移一次資料。

## 不管選哪個方案都要加的東西

### 後端 API
- `GET /api/v1/social/carousel`（`AllowAnonymous`）：回傳目前該顯示的跑馬燈項目（合併公告與活動，依方案排序），給貼文牆用。
- 方案 A：`PUT /api/v1/admin/posts/{id}/featured`、`PUT /api/v1/admin/events/{id}/featured`（沿用現有 admin controller 的 pattern）。
- 方案 B：`POST /api/v1/admin/carousel`、`DELETE /api/v1/admin/carousel/{id}`、`PUT /api/v1/admin/carousel/reorder`。

### 前端
- 貼文牆（`QMAH.Client/src/app/features/social/posts/`）加一個跑馬燈元件（daisyUI 有現成的 `carousel` class 可以用，或自己刻一個固定秒數輪播的元件）。
- 新增後台頁面 `/admin/carousel`（管理員選哪些公告/活動要放上去），比照現有 `admin-events`/`admin-reports` 的頁面風格。
- `social-api.ts` 加對應的 service 方法。

## 今天先做到哪

今天完全沒動資料庫，這份文件先放著給你開會討論方案 A/B，確認後我再依決定的方案動手（含實際執行 SQL 變更）。
